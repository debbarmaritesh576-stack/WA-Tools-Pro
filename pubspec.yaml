name: wa_tools_pro
description: All-in-one WhatsApp Tools App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  url_launcher: ^6.2.2
  share_plus: ^7.2.1
  clipboard: ^0.1.3
  shared_preferences: ^2.2.2
  permission_handler: ^11.1.0
  photo_manager: ^3.0.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
